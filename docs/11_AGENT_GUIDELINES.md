## Design Rules

Before implementing any UI changes, read:

docs/08_DESIGN_SYSTEM.md

Never introduce new colors.

Never hardcode colors.

Always use CSS variables.

Always follow the Design System.

Do not modify Bootstrap defaults directly.

Override Bootstrap using CSS variables only.

All new UI components must match the design system.